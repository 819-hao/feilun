<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seeease.flywheel.serve.financial.mapper.FinancialInvoiceRecordMapper">

    <select id="getPage" resultType="com.seeease.flywheel.financial.result.FinancialInvoiceRecordPageResult">
        select *
        from financial_invoice_record
        <where>
            deleted = 0
            <if test="request.financialInvoiceId != null">
                and financial_invoice_id = #{request.financialInvoiceId}
            </if>
        </where>
        order by id desc
    </select>
</mapper>
