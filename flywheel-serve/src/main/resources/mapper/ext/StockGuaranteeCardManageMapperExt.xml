<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seeease.flywheel.serve.goods.mapper.StockGuaranteeCardManageMapper">

    <update id="allocateOutByStockId">
        UPDATE stock_guarantee_card_manage
        SET allocate_no = #{allocateNo},
        allocate_state = 1,
        out_time = NOW()
        WHERE stock_id IN (
        <foreach collection="stockIdList" item="stockId" separator=",">
            #{stockId}
        </foreach>
        )
    </update>

    <update id="allocateInByStockId">
        UPDATE stock_guarantee_card_manage
        SET allocate_no = NULL,
        allocate_state = 0,
        out_time = NULL
        WHERE stock_id IN (
        <foreach collection="stockIdList" item="stockId" separator=",">
            #{stockId}
        </foreach>
        )
    </update>

    <update id="allocateCancel">
        UPDATE stock_guarantee_card_manage
        SET allocate_no    = NULL,
            allocate_state = 0,
            out_time       = NULL
        WHERE allocate_no = #{allocateNo}
    </update>
</mapper>
